{
  "hooks": {
    "PostToolUse": [
      {
        "type": "command",
        "event": "edit_file",
        "command": "npx eslint --fix ${filePath}",
        "windows": "npx eslint --fix ${filePath}",
        "timeout": 15
      }
    ]
  }
}
